Can you tell what is wrong with this code?
```
#include <stdlib.h>
#include <stdio.h>

int main() {
    char *x = malloc(1);
    sprintf(x, "A");
    printf("%s", x);
    free(x);
    return 0;
}
```
